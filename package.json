{
  "name": "figgie",
  "version": "0.1.0",
  "private": true,
  "description": "Figgie — a drag-to-pose stick-figure rig in a canvas. WebGL, zero dependencies, fully encapsulated.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
